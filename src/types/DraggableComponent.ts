interface DraggableComponent {
  id: string;
  type: string;
  title: string;
  inputed: boolean;
  required: boolean;
  placeholder: string;
}

interface FormProperties {
  formTitle: string;
  formSubmitPlaceholder: string;
}
